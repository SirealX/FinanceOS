# Finance Tracker — Backend Dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.13.3

# Auth / JWT (Supabase JWT verification)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Environment variables
python-dotenv==1.0.1

# Data validation (included with FastAPI but pinned for clarity)
pydantic==2.9.2

# File uploads — required by FastAPI UploadFile / File in import_router.py
python-multipart==0.0.9

# XLSX parsing — required by file_parser.py for bank statement imports
openpyxl==3.1.5

# Date arithmetic — required by recurring.py (/log endpoint advances next_due)
python-dateutil==2.9.0.post0

# Notifications — required by notifications.py
httpx==0.27.2
pywebpush==2.0.0

# Email ingestion (item #6) uses imaplib + email from the Python standard
# library — no dependency needed. Previously used google-api-python-client +
# google-auth for the Gmail API; migrated to plain IMAP + a Gmail App
# Password 2026-08-12 (see Tracker.md "Email Ingestion Pipeline") to avoid
# Google's OAuth "Testing" 7-day token expiry / "In production" brand
# verification requirements, both dead ends for a single-user tool.
